runtime: nodejs10

instance_class: F1

handlers:
  - url: /api/.*
    secure: always
    script: auto
  - url: /
    secure: always
    static_files: client/build/index.html
    upload: client/build/index.html
  - url: /
    secure: always
    static_dir: client/build
